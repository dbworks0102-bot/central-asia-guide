あなたは `c:\Users\vanva\Clode Code\central-asia-guide` （日本人旅行者向けウズベキスタン専門観光サイト、本番 https://tabi-uzbekistan.com）の
SEOコラム記事の下書きを1本作成するタスクを、無人スケジュール実行として任されています。人間のレビュー前提なので、
必ず `status: "draft"` として追加してください（`"published"` にしてはいけません）。

## 手順

1. `docs/seo-keywords.md` を読み、チェックが付いていない（`- [ ]`）キーワード項目を1つ選ぶ。
2. `src/data/articles.js` を読み、既存の `slug`・`keywords` と重複しないことを確認する。
3. 選んだキーワードのトピックにビザ・治安・料金・開業日・就航状況など**事実性の高い情報**が含まれる場合は、
   必ずWebSearchで裏取りする。裏取りできない/古い可能性がある情報は書かない。曖昧な場合は「最新情報は公式情報を
   確認してください」と明記する。**新しい事実を捏造しない**こと。
4. `public/images/uzbekistan/` 配下の既存画像（Globで確認、`inbox/` サブフォルダも含む）から、記事内容に合う
   実写真を `heroImage` に選ぶ（新規画像は作らない・生成しない）。
   - `inbox/` はユーザーがGoogleドライブにアップロードした新着写真の取り込み先で、ファイル名が
     `IMG_1234.jpg` のように内容を表していないことがある。`inbox/` 内の画像を候補にする場合は、
     ファイル名だけで判断せず、Readツールでその画像ファイルを直接見て何が写っているか（建物名・場所など）を
     必ず確認すること。確信が持てない場合はその画像を選ばず、他の画像を使うか `heroImage` を
     `hero.jpg`（サイト共通の汎用写真）にする。
   - `inbox/` の画像を採用した場合は、何が写っていると判断したか（判断根拠）を手順8の要約に必ず記載する
     （画像の内容誤認は人間レビューで確認できるようにするため）。
   - `inbox/` は一方向pull（追加のみ）で運用され、過去に採用済みの写真も削除されずそのまま残っている場合が
     ある。この取り込みは候補を増やすだけで、採用可否の判断はこのタスク（と人間レビュー）に委ねられている。
     inbox運用ルールの詳細は `docs/design.md` の §8.5.5 を参照。
5. `src/data/articles.js` の既存記事オブジェクトと同じスキーマ（`slug`, `title`, `description`, `keywords`,
   `heroImage`, `publishDate`, `status`, `body`, `relatedLinks`）に厳密に合わせて、新しい記事オブジェクトを
   配列の末尾に追記する（Editツールで直接編集。既存記事は変更しない）。
   - `status` は必ず `"draft"`。
   - `publishDate` は実行日（今日の日付、ISO形式）。
   - `body` は既存記事と同じブロック形式（`heading`/`paragraph`/`list`）。
   - **本文の文字数（`body` 内の `text`/`items` の合計、見出し文字列を含む）はおよそ2,000〜3,000文字を目安にする。**
     大きく不足・超過する場合は見出しや段落を増減して目安内に収める。
   - `relatedLinks` はサイト内の `/` または `/about` へのリンクを最低1つ含める。
6. **セルフレビュー**：Taskツールで独立したレビュー担当のサブエージェントを起動し、
   手順5で作成した記事本文を次の観点でチェックさせる。
   - 文字数がおよそ2,000〜3,000文字の目安に収まっているか
   - ビザ・治安・料金・開業日・就航状況など事実性の高い記述に、WebSearchでの裏取り根拠があるか
     （裏取りできない/古い可能性がある情報が書かれていないか、該当箇所に適切なヘッジ表現があるか）
   - 既存記事や事実と矛盾する記述がないか
   - スキーマ（フィールド名・型・`status:"draft"`）が既存記事と一致しているか
   レビューで問題が指摘された場合は、Editツールで自分で修正してから次に進む。修正しても解決できない
   重大な懸念が残る場合は、記事を追記済みのまま `status: "draft"` を維持し、その懸念を手順8の要約に明記する
   （下書きを削除する必要はない。人間レビューで判断する）。
7. `docs/seo-keywords.md` の該当項目にチェックを入れ、末尾に ` → 下書き済み: <slug>` を追記する。
8. 最後に、生成した記事の `slug`・`title`・使用したキーワード・本文の文字数・セルフレビューの結果
   （指摘の有無、修正したかどうか）・`heroImage`に選んだ画像（`inbox/`由来なら手順4の判断根拠も）を
   数行で要約して出力する（これがログに残る）。

## 制約
- **この実行で作成する記事は必ず1本だけ**。手順1〜8を1周したら終了し、続けて2本目以降を作成しない。
  （`docs/seo-keywords.md` に未使用項目が複数残っていても、選ぶのは1つだけ。）
- ビルド・テスト・git操作は一切行わない（実行不可）。
- `src/data/articles.js` と `docs/seo-keywords.md` 以外のファイルは変更しない。
- 既存の `status: "published"` の記事は絶対に変更しない。
- 手順8の要約には、この実行で `src/data/articles.js` に追記した記事の**件数**も明記すること（例:「1本作成」）。
  1本以外の件数になった場合は、その旨と理由を要約に必ず含めること。
